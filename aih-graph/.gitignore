# Built binaries
bin/

# Claude Code hook audit logs (created at runtime; per-machine state)
.claude/

# Go module cache (not normally committed)
# vendor/ is intentionally NOT here — if vendoring is adopted, vendor/ stays tracked

# OS / editor noise
.DS_Store
*.swp
*~
.vscode/
.idea/

# Test outputs
*.test
*.out
coverage.out
coverage.html

# SQLite databases produced by aih-graph build
*.db
*.db-wal
*.db-shm
*.db-journal
